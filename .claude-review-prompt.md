DO NOT ask clarifying questions. Do NOT commit or push any changes.

Review the newly created consulting services page against the existing pattern pages to ensure it follows the same conventions.

## What to review

1. **`src/pages/consulting/index.astro`** — the new consulting page
2. **`src/pages/index.astro`** — homepage (check the new card, hero tagline, JSON-LD)
3. **`src/layouts/Layout.astro`** — nav and footer

## Comparison targets

Read these files for pattern reference:
- `src/pages/travel/index.astro` — existing service page
- `src/pages/pet-care/index.astro` — existing service page
- `src/pages/index.astro` — read the existing three cards and the hero section

## Specific things to check

1. **CSS class conventions** — Does the consulting page use the same hero classes (`.hero`, `.hero-image`, `.hero-inner`, `.hero-title`, `.hero-tagline`, `.hero-cta`, `.hero-scroll`) as travel/pet-care?

2. **Animation classes** — Are `.rise` and `data-delay` applied consistently? Is the stagger sequence correct?

3. **Semantic HTML** — Are `aria-labelledby`, `aria-label`, `role` attributes used consistently with travel/pet-care?

4. **Layout/Props** — Does `overlayHeader` match the pattern? Are OG meta tags structured the same way?

5. **Image patterns** — Are `loading="lazy"`, `fetchpriority`, `width`/`height`, and `alt` text used consistently?

6. **Nav active state** — Does `aria-current="page"` match the pattern used by travel and pet-care nav links?

7. **JSON-LD structure** — Is the consulting page's structured data following the same pattern as travel and pet-care?

8. **Homepage card** — Does the new consulting card on index.astro use the exact same `.card`, `.card-media`, `.card-body`, `.card-footer` structure as the existing three cards?

9. **Footer** — Is the footer link using the same structure as other footer links?

10. **Button patterns** — Are `.btn`, `.btn-primary`, `.btn-link` used consistently?

## Output format

Categorize findings as:
- ✅ **Pass** — matches pattern correctly
- ⚠️ **Minor** — works but doesn't match the pattern (suggest fix)
- ❌ **Fail** — broken or missing

For each finding, cite the specific line numbers or patterns.

Do NOT make any changes — just review and report findings.
