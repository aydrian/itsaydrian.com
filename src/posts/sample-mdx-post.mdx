---
title: "MDX Pipeline Test Post"
excerpt: "A short post confirming the Astro MDX integration renders inside the existing blog layout."
date: "2025-12-01"
category: "Life in NYC"
slug: "mdx-pipeline-test"
---

This post is written in **MDX**. It should render inside the same `Layout` and post wrapper as the existing JavaScript-driven posts.

## What to check

- The post appears on the [blog index](/blog/).
- The category pill, date, title, and excerpt all render.
- This heading and list use the existing `.prose` styles.
- Inline **bold**, _italic_, and [links](/) still work.

If this page looks like the others, the MDX pipeline is configured correctly.
